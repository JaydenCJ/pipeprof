// pipeprof — profile shell pipelines per stage: bytes, records, wall
// time, CPU time, exit codes.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/pipeprof
// keywords:   pipeline, profiling, shell, throughput, bottleneck, cli, unix
//
// Zero runtime dependencies: the require list below is intentionally empty
// and must stay that way (see CONTRIBUTING.md, "Ground rules").
module github.com/JaydenCJ/pipeprof

go 1.22
